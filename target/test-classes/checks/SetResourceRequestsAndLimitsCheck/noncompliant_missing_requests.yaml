apiVersion: apps/v1
kind: Deployment
metadata:
  name: api
spec:
  template:
    spec:
      containers:
        - name: api
          image: myorg/api@sha256:abc
          resources: # Noncompliant {{Set resources.requests.cpu and resources.requests.memory to enable deterministic scheduling.}}
            limits:
              memory: "256Mi"
