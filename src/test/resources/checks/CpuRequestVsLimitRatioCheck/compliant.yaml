apiVersion: v1
kind: Pod
metadata:
  name: p
spec:
  containers:
    - name: ok
      image: myorg/api:1
      resources:
        requests:
          cpu: "100m"
          memory: "128Mi"
        limits:
          cpu: "200m"
          memory: "256Mi"
    - name: only-requests
      image: myorg/api:1
      resources:
        requests:
          cpu: "100m"
          memory: "128Mi"

